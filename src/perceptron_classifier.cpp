#include "perceptron_classifier.h"

#include <string>
#include <vector>

#include "pitch.h"
#include "pitch_set.h"

using namespace pitch_classifier;

void PerceptronClassifier::train(PitchSet* training_data) {

  
}

std::vector<std::string> PerceptronClassifier::test(PitchSet* test_data) {
  std::vector<std::string> p;
  return p;
}

std::string PerceptronClassifier::classify(Pitch p) {
  return "";
  
}
